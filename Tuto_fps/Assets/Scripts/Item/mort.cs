using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class mort : MonoBehaviour {

	/*int damping = 6;

	// Use this for initialization
	void Start () {
		while ((transform.rotation.z < 80) && (transform.position.y > 0.1)){

			transform.rotation.z = 10f;
			transform.position.y -= 0.1f;

			Quaternion posi = Quaternion.LookRotation (transform.position.y - 0.1f);
			transform.position = Quaternion.Slerp (transform.position, posi, Time.deltaTime * damping);

			Quaternion rota = Quaternion.LookRotation (transform.position.y - 10f);
			transform.rotation = Quaternion.Slerp (transform.rotation, rota, Time.deltaTime * damping);
		}
	}*/
}
